import { Queryable } from '@databases/mysql';
export interface ConstraintQuery {
    schemaName?: string;
    tableName?: string;
}
export interface Constraint {
    schemaName: string;
    tableName: string;
    constraintName: string;
    columns: {
        columnName: string;
        referenced: null | {
            schemaName: string;
            tableName: string;
            columnName: string;
        };
    }[];
}
export default function getTables(connection: Queryable, query: ConstraintQuery): Promise<Constraint[]>;
